export type {
  ResumeProfile,
  ExperienceEntry,
  ProjectEntry,
  EducationEntry,
} from '@/lib/schemas/resume';

export type {
  JobDescriptionProfile,
} from '@/lib/schemas/job-description';

export type {
  MatchScore,
} from '@/lib/schemas/match-score';

export type {
  TailoredBullet,
  TailoredExperience,
  TailoredResume,
} from '@/lib/schemas/tailored-resume';

export type {
  ResumeGap,
  GapAnalysis,
} from '@/lib/schemas/gap-analysis';

export type {
  TailoringRun,
} from '@/lib/schemas/tailoring-run';
